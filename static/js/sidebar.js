












});  });    format: 'yyyy/mm/dd'  $('#attendanceDate').datepicker({  // Apply any dashboard-specific initializations    }    $('#courseSubmenu').parent().find('a.dropdown-toggle').attr('aria-expanded', 'true');    $('#courseSubmenu').addClass('show');  if (window.location.pathname.includes('/course-faculty')) {  // If current URL matches Course & Faculty routes, expand the submenu$(document).ready(function() {// Initialize the Course & Faculty Management submenu toggle